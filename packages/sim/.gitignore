# FastF1 raw cache (regenerable, large) — not committed
src/formation_sim/data/cache/
# Runtime report output
src/formation_sim/output/
__pycache__/
*.pyc
.pytest_cache/

# Derived tables are regenerable and not committed — except the dated fitted-parameter
# artifact, which the sim loads at runtime (e.g. in CI where the FastF1 cache is absent).
src/formation_sim/data/derived/*
!src/formation_sim/data/derived/paramset08072026.pkl